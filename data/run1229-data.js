// R1229 — BUG FIX: Fix XP values + recordActivity args in hrm/syd/mfl/tyn pickOpts
// hrm/syd: correct gives addXp(2) instead of addXp(10), wrong gives nothing
// mfl/tyn: correct gives addXp(2) instead of addXp(10), wrong gives addXp(1) instead of addXp(2)
// hrm/syd/mfl/tyn: recordActivity() missing coach name argument
